import { PreventKeysDirective } from './prevent-keys.directive';

describe('PreventKeysDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventKeysDirective();
    expect(directive).toBeTruthy();
  });
});
